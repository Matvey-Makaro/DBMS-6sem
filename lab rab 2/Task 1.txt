CREATE TABLE STUDENTS(
ID NUMBER,
NAME VARCHAR2(50),
GROUP_ID NUMBER,
CONSTRAINT STUDENTS_PK PRIMARY KEY (ID),
CONSTRAINT GROUPS_FK FOREIGN KEY (GROUP_ID)
REFERENCES GROUPS (ID)
);


CREATE TABLE GROUPS(
ID NUMBER,
NAME VARCHAR2(50),
C_VAL NUMBER,
CONSTRAINT GROUPS_PK PRIMARY KEY(ID)
);